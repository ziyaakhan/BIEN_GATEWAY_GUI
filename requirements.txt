# FastAPI ve Web Server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Dosya izleme (File Watching)
watchdog>=3.0.0

# Serial port (RS-485 için)
pyserial>=3.5

# Modbus (opsiyonel - RS-485 Modbus için)
# pymodbus>=3.5.0

# BLE (Raspberry Pi için - bluepy önerilir)
bluepy>=1.3.0
# Alternatif: bleak (cross-platform, async)
# bleak>=0.21.0

# MQTT (BLE ve LoRaWAN için)
paho-mqtt>=1.6.0

# HTTP/HTTPS istekleri için
requests>=2.31.0

# JSON işlemleri için (Python standart kütüphanesi ile gelir, ama jq benzeri için)
# jq>=1.0.0
